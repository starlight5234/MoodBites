// import React, { useState } from 'react';
// // import React from 'react'
// import CollectionCard from '../Components/Cards/CollectionCard/CollectionCard'

const Demo = () => {
    

    return (
        <>
            {/* <div className="flex items-center">
                {[1, 2, 3, 4, 5].map((value, index) => (
                    <React.Fragment key={value}>
                        <label className="relative">
                            <input
                                type="radio"
                                name="rating"
                                value={value}
                                className="sr-only"
                                checked={rating === value}
                                onChange={() => handleRatingChange(value)}
                            />
                            <span
                                className={`flex align-middle rounded-full inline-block mx-1 cursor-pointer ${rating <= value ? 'bg-yellow-400' : 'bg-gray-300'
                                    } ${rating == value ? 'w-4 h-4' : 'w-3 h-3'
                                }`}
                            />
                        </label>
                        {index < 4 && <div className="w-6 h-px bg-gray-300" />}
                    </React.Fragment>
                ))}
            </div>
            <span>{rating}</span> */}
        </>
    );
}

export default Demo
